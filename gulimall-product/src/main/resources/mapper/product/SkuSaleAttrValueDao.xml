<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wzh.gulimall.product.dao.SkuSaleAttrValueDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.wzh.gulimall.product.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>
<!--    <select id="getSaleAttrsBySpuId" resultType="com.wzh.gulimall.product.vo.SkuItemVo$SkuItemSaleAttrVo">-->
<!--        SELECT saav.attr_id attr_id, saav.attr_name attr_name, GROUP_CONCAT(DISTINCT saav.attr_value) attr_values-->
<!--        FROM pms_sku_info si-->
<!--        LEFT JOIN pms_sku_sale_attr_value saav-->
<!--        ON si.sku_id = saav.sku_id-->
<!--        WHERE spu_id = #{spuId}-->
<!--        GROUP BY saav.attr_id, saav.attr_name-->

<!--    </select>-->

    <resultMap id="skuItemSaleAttrVo" type="com.wzh.gulimall.product.vo.SkuItemVo$SkuItemSaleAttrVo">
        <result column="attr_id" property="attrId"></result>
        <result column="attr_name" property="attrName"></result>
        <collection property="attrValues" ofType="com.wzh.gulimall.product.vo.SkuItemVo$AttrValueWithSkuIdVo">
            <result column="attr_value" property="attrValue"></result>
            <result column="sku_ids" property="skuIds"></result>
        </collection>
    </resultMap>

    <select id="getSaleAttrsBySpuId" resultMap="skuItemSaleAttrVo">
        SELECT
            ssav.attr_id attr_id,
            ssav.attr_name attr_name,
            ssav.attr_value,
            group_concat( DISTINCT info.sku_id ) sku_ids
        FROM
            pms_sku_info info
                LEFT JOIN pms_sku_sale_attr_value ssav ON ssav.sku_id = info.sku_id
        WHERE
            info.spu_id = #{spuId}
        GROUP BY
            ssav.attr_id,
            ssav.attr_name,
            ssav.attr_value

    </select>


</mapper>